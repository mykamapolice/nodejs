## Open your terminal and tab `npm install` to install dependences
## Fter installing dependences tab `npm run start:dev` to launch app in develop mode 
## or `npm run start:prod` to launch app in prod mode